# Core scientific computing
numpy>=1.26,<3.0
scipy>=1.11,<2.0
librosa>=0.10,<1.0
soundfile>=0.12,<1.0

# ML / dimensionality reduction
scikit-learn>=1.3,<2.0
umap-learn>=0.5,<1.0

# Visualization
plotly>=5.18,<6.0

# Web framework
# Pinned to a single minor. The previous `>=0.110,<1.0` spanned 30 minor
# versions of a library that changes routing internals inside that range:
# CI resolved fastapi 0.140 / starlette 1.3.1 while local venvs still had
# 0.128 / 0.50, and `app.routes` started yielding objects with no `.path`.
# Tests passed locally and failed in CI on code nobody had touched.
fastapi>=0.140,<0.141
# Transitive, pinned explicitly: starlette 0.50 -> 1.3.1 is a MAJOR bump and
# it is the dependency that actually broke. It must not float silently again.
starlette>=1.3,<1.4
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.9,<1.0
jinja2>=3.1,<4.0

# API validation
pydantic>=2.0,<3.0

# Auth & payments
pyjwt>=2.8,<3.0
stripe>=8.0,<13.0

# Testing
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
httpx>=0.27,<1.0
