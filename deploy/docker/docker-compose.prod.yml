services:
  api:
    build: .
    ports: ['8000:8000']
  worker:
    build:
      dockerfile: deploy/docker/Dockerfile.worker
  redis:
    image: redis:7-alpine
  postgres:
    image: postgres:16-alpine
